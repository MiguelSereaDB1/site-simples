# Gestão de Roupas

Monorepo com backend em FastAPI (`backend/`) e frontend em Next.js (`frontend/`).

## Desenvolvimento

```bash
./dev.sh
```

Sobe backend e frontend juntos. `Ctrl+C` encerra os dois.

Para rodar cada lado separadamente, veja `backend/README.md` e `frontend/README.md`.
