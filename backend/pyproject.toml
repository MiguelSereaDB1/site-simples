[project]
name = "fastapi-app"
version = "0.1.0"
description = "Minimal FastAPI app for CI/CD smoke tests"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.139.0",
]
